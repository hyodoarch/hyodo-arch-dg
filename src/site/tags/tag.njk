<h2 class="tag-page-title">{{ tagName | escape }}</h2>
<p class="tag-page-description">{{ tagName | escape }}の事例をまとめました。</p>
{% if tagNotes and tagNotes.length > 0 %}
  {% set listingNotes = tagNotes %}
  {% include "components/note-cards.njk" %}
{% else %}
  <p>このタグのノートはありません。</p>
{% endif %}
